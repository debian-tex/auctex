#include "variables"
Template: <:=${PACKAGE}:>/default
Type: boolean
Default: true
Description: Should AUC-TeX be auto-loaded by default at your site?
 Emacs has its own standard (La)TeX major modes, that will be globally (i.e.,
 for all users on this site) overridden by AUC-TeX if you answer yes to this
 question.
 .
 This is generally a good thing, since AUC-TeX is so much better; however you
 may not want it to happen, and instead let single users at your site decide
 by themselves if they should load this package.
 .
 If you answer no, people who desire to use it will have to put the string
 "(require 'tex-site)" in their "~/.emacs" to load it.
Description-it: Abilitare AUC-TeX automaticamente su questa macchina?
 Emacs normalmente ha i propri modi maggiori originali per (La)TeX; se si
 risponde affermativamente a questa domanda, questi verranno globalmente
 sostituiti da quelli di AUC-TeX (sostituiti, cioè, per tutti gli utenti di
 questa macchina).
 .
 In genere questa è una buona cosa, perché i modi di AUC-TeX sono molto
 migliori degli originali; può darsi, tuttavia, che si preferisca lasciare a
 ciascun singolo utente la decisione di far uso o meno di questo pacchetto.
 .
 Se la risposta sarà negativa, gli utenti che ne vorranno far uso dovranno
 mettere manualmente la stringa "(require 'tex-site)" nel loro "~/.emacs" per
 abilitare AUC-TeX.
Description-de: Soll AUC-TeX systemweit automatisch geladen werden?
 Emacs hat standardmäßige (La)TeX-Modi, die global (also für alle Nutzer auf
 Ihrem System) überschrieben werden, wenn Sie diese Frage bejahen.
 .
 Das ist generell ein gutes Ding, da AUC-TeX 'ne Ecke besser ist -- wie auch
 immer, Sie könnten das nicht mögen und jedem Nutzer die Wahl lassen wollen,
 ob sie das Paket einbinden.
 .
 Wenn Sie die Frage verneinen, müssen Nutzer, die AUC-TeX nutzen wollen,
 "(require 'tex-site)" in ihr "~/.emacs" einfügen.
Description-es: ¿Debe cargarse AUC-TeX por defecto en su sistema?
 Emacs tiene sus modos (La)TeX estándar que serán reemplazados globalmente
 (para todos los usuarios del sistema) por AUC-TeX si responde Sí a esta
 pregunta.
 .
 Generalmente es una buena idea ya que AUC-TeX es mucho mejor.  Sin embargo
 puede que no le guste la idea y prefiera que los usuarios de su sistema
 decidan por sí mismos si quieren cargar este paquete.
 .
 Si responde No la gente que quiera usarlo deberá incluir la cadena
 "(require 'tex-site)" en su "~/.emacs" para cargarlo.

Template: <:=${PACKAGE}:>/alreadydefault
Type: note
Description: AUC-TeX seems to be already auto-loaded at your site.  Good.
 By inspection of the file "<:=${SSEL}:>" AUC-TeX seems to be
 already auto-loaded by default at your site.
 .
 If this is not true, or if it's not what you want, please search that file
 looking for "(require 'tex-site)" resembling forms and fix them; then run
 "dpkg-reconfigure <:=${PACKAGE}:>" if needed (recommended).
Description-it: AUC-TeX sembra già automaticamente abilitato su questa macchina.  Bene.
 Controllando il file "<:=${SSEL}:>", pare che AUC-TeX sia già automaticamente
 abilitato su questa macchina.
 .
 Se questo non è vero, o se non è quel che si desidera, si vadano a cercare in
 quel file Lisp form del tipo "(require 'tex-site)" e li si correggano;
 poi si faccia girare "dpkg-reconfigure <:=${PACKAGE}:>" per configurare di
 nuovo AUC-TeX.
Description-de: AUC-TeX scheint schon standardmäßig eingebunden zu sein. Fein.
 Nach Kontrolle der Datei "/etc/emacs/site-start.el" scheint AUC-TeX bereits
 standardmäßig auf Ihrem System eingebunden zu werden.
 .
 Wenn das nicht stimmt oder wenn das nicht das ist, was Sie wollen, dann
 entfernen Sie bitte die "(require 'tex-site)"-Aufrufe in der genannten
 Datei. Danach wird der Aufruf von "dpkg-reconfigure auctex" empfohlen.
Description-es: Parece que AUC-TeX ya se está cargando automáticamente. Bien
 Tras inspeccionar el fichero "/etc/emacs/site-start.el" parece que AUC-TeX
 esta ya configurado para cargarse por defecto en su sistema.
 .
 Si no es verdad o si no lo quiere, por favor, busque en el fichero la frase
 "(require 'tex-site)" y modifíquela.
 Después ejecute "dpkg-reconfigure auctex" si es necesario (recomendado).

Template: <:=${PACKAGE}:>/doauto
Type: select
Choices: Background, Foreground, None
Choices-it: Sottofondo, Primo piano, Nessuno
Choices-es: Segundo plano, Primer plano, Omitir
Default: Background
Description: How should (La)TeX macros be parsed?
 To greatly improve AUC-TeX performances, every TeX macro package and LaTeX
 style file currently installed at your site will be parsed.
 .
 This may take a lot of time, so it should be probably done in background;
 however you may also choose to have it done in foreground, or to skip the
 parsing at all instead.  If you choose Background, you will find a detailed
 log of the process in the file: ${LOGFILE}.
 .
 A weekly cron job will also take care of updating the informations now
 collected, so usually you won't need to worry about them whenever you install
 new (La)TeX packages or remove old ones.
 .
 Anyway, you can update them by hand running <:=${UPDATE}:>.
Description-it: In che modo si deve effettuare la scansione delle macro per (La)TeX?
 Per migliorare grandemente le proprie prestazioni, AUC-TeX scandirà tutti i
 pacchetti di macro per TeX ed i file di stile per LaTeX attualmente
 installati su questa macchina.
 .
 Questa operazione potrebbe richiedere molto tempo ed è quindi probabilmente
 preferibile eseguirla in sottofondo; tuttavia, si può anche scegliere di
 eseguirla in primo piano o di non eseguirla affatto.  Se si sceglie
 Sottofondo verrà generata una cronistoria dettagliata dell'operazione nel
 file: ${LOGFILE}.
 .
 Le informazioni raccolte adesso saranno aggiornate settimanalmente per mezzo
 del demone cron, di modo che normalmente non sarà necessario preoccuparsene
 di persona al momento dell'installazione o della rimozione di pacchetti
 aggiuntivi per (La)TeX.
 .
 In ogni caso, le informazioni si possono sempre aggiornare a mano lanciando
 <:=${UPDATE}:>.
Description-de: Wie sollen (La)TeX-Makros behandelt werden?
 Zur Beschleunigung von AUC-TeX kann jedes installierte TeX-Makro und jeder
 LaTeX-Style im Voraus bearbeitet werden.
 .
 Das kann eine Weile dauern, daher wird die Bearbeitung im Hintergrund
 empfohlen. Die Bearbeitung kann allerdings auch im Vordergrund erfolgen --
 die weitere Installation wird dann allerdings bis zum Abschluß der
 Bearbeitung unterbrochen. Die Voraus-Bearbeitung kann auch ganz
 unterbleiben. Wenn Sie die Bearbeitung im Hintergrund wählen, finden Sie
 eine detaillierte Protokollausgabe in ${LOGFILE}.
 .
 Weiterhin wird ein wöchentlicher cron-Job auf die Fortschreibung der nun
 erhobenen Informationen achten, so daß Sie sich für gewöhnlich nicht weiter
 darum kümmern müssen, wenn Sie LaTeX-Pakete installieren oder entfernen.
 .
 Die Aktualisierung kann zudem von Hand durch Aufruf von
 "update-auctex-elisp" erfolgen.
Description-es: ¿Como deben analizarse las macros (La)TeX?
 Para mejorar el rendimiento de AUC-TeX se analizará el contenido de cada
 paquete de macros TeX y ficheros de estilo LaTeX instalado en su sistema.
 .
 Esto puede tardar mucho tiempo, por lo que se debería hacer en segundo
 plano.  Sin embargo puede elegir hacerlo en primer plano u omitir este
 paso.  Si elige hacerlo en segundo plano encontrará un detallado registro
 del proceso en el fichero: ${LOGFILE}.
 .
 Así mismo, una tarea semanal actualizará la información recolectada en esta
 sesión por lo que no se tiene que preocupar cuando instale nuevos paquetes
 de (La)TeX o borre los antiguos.
 .
 De cualquier forma puede actualizarlos ejecutando update-auctex-elisp en
 cualquier momento.

Template: <:=${PACKAGE}:>/doautofg
Type: select
Choices: Console, File
Choices-it: Video, File
Choices-es: Consola, Fichero
Default: File
Description: Where should parsing output go?
  You chose to parse TeX macro packages and LaTeX style files in foreground.
  This is ok, but normally it will also generate quite a lot of output.
  .
  By default -- i.e., answering File to this question -- further output from
  the parsing process will go to a file named: ${LOGFILE}.
  .
  If you answer Console, you will see all of the (boring) output right on your
  terminal.
Description-it: Dove vanno inviati i messaggi della scansione?
  I pacchetti di macro per TeX ed i file di stile per LaTeX saranno scanditi
  in primo piano come è stato richiesto.  Questo va bene, ma di solito produce
  molti messaggi informativi.
  .
  Rispondendo File a questa domanda i messaggi originati dalla scansione
  verranno scritti in un file di nome ${LOGFILE}.
  .
  Rispondendo Video, i messaggi (noiosi) verranno visualizzati sul proprio
  terminale.
Description-de: Wo soll die Protokoll-Ausgabe hingehen?
 Sie wählten die Bearbeitung der Tex-Makros und LaTeX-Styles im Vordergrund.
 Das ist schon in Ordnung, aber für gewöhnlich wird dabei eine Menge
 langweiliger Kram ausgegeben.
 .
 Standardmäßig (also bei der Wahl von "Datei") wird die weitere Ausgabe des
 Bearbeitungsprozesses in die Datei ${LOGFILE} umgeleitet.
 .
 Wenn Sie mit "Konsole" antworten, wird die gesamte Ausgabe direkt auf Ihrem
 Terminal erscheinen.
Description-es: ¿A donde se debe enviar la información al analizar?
 Ha elegido analizar los paquetes de macros TeX y ficheros de estilo LaTeX
 en primer plano.
 .
 Por defecto, respondiendo Fichero a esta pregunta, los mensajes del
 proceso de analizado se guardarán en un fichero llamado: ${LOGFILE}.
 .
 Si responde Consola, verá todos los (aburridos) mensajes en su terminal.

<:
# Local variables:
# mode: text
# fill-column: 78
# End:
# LocalWords:  AUC tex alreadydefault SSEL dpkg doauto LOGFILE cron AUC-TeX
# LocalWords:  require tex-site form dpkg-reconfigure PACKAGE UPDATE
:>//
